# Robots
Проект по изучению концепций OO-дизайна и разработке MDI-приложений на Java


Задача 1.1: Научиться собирать проект с использованием Apache Maven.

	Умение собирать код без среды разработки необходимо для того, чтобы организовать рабочий процесс — 
	код нужно регулярно собирать, проверять что он компилируется, а потом, для того чтобы убедиться в 
	работоспособности кода, прогонять тесты.
	
	Система сборки это программа, которая собирает другие программы. На вход система сборки получает 
	исходный код, а на выход выдаёт программу, которую уже можно запустить. Система сборки вызывает компилятор 
	при своей работе, а компилятор о существовании системы сборки даже не подозревает.(сборка, помимо компиляции, 
	включает в себя ещё целый спектр задач. Например, если программе для работы нужны какие-нибудь картинки, 
	то положить их в директорию с программой — задача системы сборки. Если программе нужны сторонние библиотеки, 
	то положить их в директорию с программой — задача системы сборки.)
	
	В рамках задачи сделано: 
	
		1. Создан файл конфигурации Maven — pom.xml, в нем описаны:

			- Идентификаторы проекта (groupId, artifactId, version)
			- Версия Java для компиляции и запуска (21)
			- Расположение исходного кода (папка src/)
			- Главный класс приложения (gui.RobotsProgram)
			- Плагин для создания исполняемого JAR-файла

		2. Настроена система сборки:
		
			- Проект собирается командой mvn clean compile:
				    clean — удаляет предыдущие результаты сборки
					compile — компилирует исходный код в байт-код (.class файлы)
			
			- Создаётся исполняемый JAR-файл командой mvn package:
					Упаковывает скомпилированные классы в JAR-архив
					Результат: target/Robots-X.X-SNAPSHOT.jar


Задача 1.2: Провести рефакторинг и исправить баг.

	Первая часть задачи - провести рефакторинг. Метод создания меню перегружен функционалом и трудно читается. Следует разделить его на серию более простых методов.
	
	В рамках первой части задачи сделано:
		
		1. Разделён один сложный метод generateMenuBar разделён на серию простых методов. 
	
	Вторая часть задачи - исправить баг. В исходной реализации была обнаружена утечка памяти, 
	связанная с неправильной работой слушателей событий лога. Когда пользователь закрывал окно, LogWindow оставался 
	в памяти, потому что LogWindowSource продолжал хранить на него ссылку в списке слушателей. Сборщик мусора не мог 
	удалить объект, так как на него была активная ссылка. При каждом новом сообщении вызывался метод onLogChanged() у 
	закрытого (уже невидимого) окна.
	
	В рамках второй части задачи сделано:
	
		1. Добавлена отписка от лога при закрытии окна (LogWindow.java) (Теперь при закрытии окна оно явно отписывается 
		   от источника логов, удаляя себя из списка слушателей. Это гарантирует, что закрытое окно больше не будет получать 
		   уведомления и может быть корректно удалено сборщиком мусора.)
		  
		2. Изменён тип коллекции для хранения сообщений (LogWindowSource.java) (LinkedList эффективнее удаляет первый элемент 
		  (самое старое сообщение), так как удаление в начале списка выполняется за константное время O(1), в отличие от ArrayList, 
		   где при удалении первого элемента приходится сдвигать все остальные элементы.)
		  
		3. Добавлено ограничение на количество хранимых сообщений (Теперь лог хранит только последние 100 сообщений. При добавлении 
		   нового сообщения, если размер очереди превышает лимит, самое старое сообщение автоматически удаляется. Это предотвращает 
		   бесконечный рост потребления памяти при длительной работе приложения.)
		
		4. Вместо прямых (сильных) ссылок на слушателей теперь используются слабые ссылки (WeakReference). Сборщик мусора может удалить 
		   объект, даже если на него есть слабые ссылки (в отличие от сильных, которые препятствуют удалению).
		  
		5. Добавлен кэш активных слушателей для оптимизации (Поскольку работа со слабыми ссылками требует проверки, жив ли ещё объект, 
		   эта операция может быть затратной при большом количестве сообщений. Для оптимизации добавлен кэш активных слушателей — массив, 
		   который содержит только живые объекты. Кэш формируется при первом уведомлении после изменений в списке слушателей и используется 
		   для последующих уведомлений, пока список не изменится (при добавлении или удалении слушателя кэш сбрасывается). )
		
Задача 1.3: 
	Суть - исправить баг убегания робота за границу окна.
	В исходном коде метод asNormalizedRadians приводил угол к диапазону от 0 до 2pi и 
	робот просто сравнивал, больше текущий угол или меньше целевого, и всегда поворачивал в одну сторону
	Теперь робот вычисляет разницу между angleToTarget и m_robotDirection и нормализует её так, чтобы она лежала в диапазоне от -pi до pi. 
	Если разница отрицательная — поворот влево, если положительная — вправо
	В рамках задачи сделано:
	
		1. Изменена логика подсчёта угла поворота
		
		2. Изменено ускорение робота при повороте



Задача 2.1: Добавить окно с подтверждением на закрытие любого из окон.
	
	В рамках задачи сделано: 
	
		Добавлена защита от случайного закрытия внутренних окон (лог-окна и игрового окна). При попытке 
		закрыть любое внутреннее окно появляется диалог подтверждения.
		
		В класс MainApplicationFrame добавлен метод addWindowWithConfirmation(), который:

			- Отключает стандартное закрытие окна (DO_NOTHING_ON_CLOSE)
			- Добавляет слушатель InternalFrameListener, перехватывающий событие закрытия
			- Показывает диалог подтверждения через метод confirmCloseFrame()
	
	
Задача 2.2: Добавить пункт меню "Выйти" (с обработкой аналогичной кнопке закрытия приложения).

	В рамках задачи сделано:
	
		1. Добавлено меню "Файл" с пунктом "Выход", который дублирует функциональность кнопки закрытия главного окна. 
		   При выборе пункта меню появляется диалог подтверждения выхода.
		
		2. Добавлены методы createFileMenu() и createExitMenuItem() для создания пунктов меню "Файл"
		   и "Выход"
		
		3. Добавлен слушатель WindowListener, перехватывающий событие закрытия и вызывающий confirmExitApplication()
		
		4. Добавлен метод confirmExitApplication(), который:
		
			- Показывает диалог подтверждения выхода из приложения
			- Записывает событие в лог
	
	
Задача 2.3: Добавить локализацию приложения (пункты меню, кнопки и т.д.).

	В рамках задачи сделано:
		
		1. Созданы файлы с переводами (src/resources/) (файлы - messages_en.properties и messages_ru.properties, в
		   файлах все текста приложения в формате ключ-значение (заголовки окон, пункты меню, текст диалогов, сообщения лога,
		   подсказки для стандартных кнопок Swing)). Для смены языка нужно добавить новый файл с переводами.
		   
		2. Создан класс Components.java для перевода стандартных элементов Swing (переводит всплывающие подсказки на кнопках
		   свернуть/развернуть/закрыть во внутренних окнах, тексты в системном меню окон (правый клик на заголовке), кнопки
		   да/нет в диалоговых окнах.
		  
		3. Создан интерфейс Localizable (все компоненты, которые должны обновляться при смене языка, реализуют этот интерфейс,
		   это позволяет обновлять все окна через один список в MainApplicationFrame)
		
		4. Изменены классы окон для поддержки локализации (GameWindow.java и LogWindow.java):
		
			- Добавлено поле private ResourceBundle bundle для хранения текущих переводов
			- Конструкторы теперь принимают ResourceBundle и устанавливают заголовок через bundle.getString()
			- Реализован метод updateLanguage() (метод интерфейса) для обновления заголовка при смене языка
		   
		5. Модифицирован класс MainApplicationFrame.java:
			- Добавлены поля для хранения текущих переводов (bundle) и для хранения текущего выбранного языка (currentLocale)
			- Добавлен список для хранения всех окон, который реализовали localizable
			- добавлен метод для переключения языка (switchLanguage()) и метод для создания меню выбора языка (createLanguageMenu())


Задача 2.4: По умолчанию выбирается язык системы, но он может быть изменён через меню.

	В рамках задачи сделано:
	
		1. Определение языка системы при запуске (RobotsProgram.java)(при запуске приложение определяет язык, установленный в операционной 
		   системе (русский, английский и т.д.), и загружает соответствующий файл с переводами)
			
		2. Создание меню выбора языка (MainApplicationFrame.java) 
			- добавлен метод для переключения языка (switchLanguage()) и метод для создания меню выбора языка (createLanguageMenu())
			- используются JRadioButtonMenuItem для наглядного отображения выбранного языка
		    - ButtonGroup гарантирует, что можно выбрать только один язык
			- Реализация переключения языка (switchLanguage()):
				- Загружается новый .properties файл для выбранного языка
				- Обновляются стандартные кнопки Swing через Components
				- Обновляются заголовки всех открытых окон
				- Пересоздаётся меню и перерисовывается интрефейс
				

Задача 3: Постановка - 
	1. В методе выхода из приложения сохранять в файл текущий профиль. 
	2. При запуске приложения следует восстанавливать профиль из файла.
	3. При запуске приложения необходимо выводить предупреждение о наличии сохранённого профиля и предлагать его восстановить (Да/Нет).
	4. Выбранная локализация должна сохраняться (в частности, влиять на содержимое окошко с предупреждением о восстановлении из п.3).
	5. Поддержать работу с несколькими профилями в п.3 (возможность выбора из нескольких сохранённых).
	
	Сделано:
	все пункты